server:
  port: 8083

spring:
  application:
      name: s21bot

bot:
  token: ${BOT_TOKEN}
  admin: ${BOT_ADMIN}

logging:
  level:
    io.github.resilience4j: INFO

profile:
  service:
    url: ${PROFILE_SERVICE_URL}
name: Test PR

on:
  pull_request:
    branches:
      - develop
      - master
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: test-pr-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  gradle-tests:
    uses: ./.github/workflows/_tests-reusable.yml

  sonar-quality:
    name: Sonar Quality
    needs: gradle-tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_PROJECT_KEY: ${{ vars.SONAR_PROJECT_KEY }}
      SONAR_ORGANIZATION: ${{ vars.SONAR_ORGANIZATION }}
      SONAR_HOST_URL: ${{ vars.SONAR_HOST_URL }}
    steps:
      - name: Skip Sonar when token is not configured
        if: env.SONAR_TOKEN == ''
        run: echo "SONAR_TOKEN is not configured, skipping Sonar analysis."

      - name: Checkout
        if: env.SONAR_TOKEN != ''
        uses: actions/checkout@v5

      - name: Setup Java 21
        if: env.SONAR_TOKEN != ''
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Setup Gradle
        if: env.SONAR_TOKEN != ''
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: Run Sonar analysis with coverage
        if: env.SONAR_TOKEN != ''
        shell: bash
        run: |
          set -euo pipefail
          if [[ -f .env ]]; then
            set -a
            source .env
            set +a
          fi
          ./gradlew runAllTestsWithCoverage sonarqube --parallel --max-workers=4 --build-cache --configuration-cache
